export declare const STREAK_KEY = "streak";
export declare type Streak = {
    startDate: string;
    lastLoginDate: string;
    currentCount: number;
};
export declare function formattedDate(date: Date): string;
export declare function buildStreakCount(date: Date): Streak;
export declare function resetStreakCount(currentStreak: Streak, date: Date): {
    startDate: string;
    lastLoginDate: string;
    currentCount: number;
};
export declare function incrementStreakCount(currentStreak: Streak, date: Date): Streak;
/**
 *
 * @param currentDate
 * @param lastLoginDate
 * returns a boolean value indicating whether or not you should increment or
 * reset streak count
 */
export declare function shouldInrementOrResetStreakCount(currentDate: string, lastLoginDate: string): {
    shouldIncrement: boolean;
    shouldReset: boolean;
};
export declare function intializeStreak(_localStorage: Storage, streak: Streak): void;
export declare function updateStreak(_localStorage: Storage, streak: Streak): void;
export declare function getStreak(_localStorage: Storage): Streak;
export declare function doesStreakExist(_localStorage: Storage): boolean;
export declare function removeStreak(_localStorage: Storage): void;
export declare function initializeAndGetStreak(_localStorage: Storage, currentDate: Date): Streak;
